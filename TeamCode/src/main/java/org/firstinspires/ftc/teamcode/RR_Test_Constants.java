package org.firstinspires.ftc.teamcode;

import com.acmerobotics.dashboard.config.Config;

@Config
public class RR_Test_Constants {
    public static double moveSpeed = 20;
    public static double minAccel = -25;
    public static double maxAccel = 25;
    public static double turnAngle = Math.PI / 2;
    public static double turnSpeed = Math.PI / 2;
    public static double minTurnAccel = -Math.PI / 2;
    public static double maxTurnAccel = Math.PI / 2;
    public static int waitTime = 2;
    public static boolean turnTest = false;
}
